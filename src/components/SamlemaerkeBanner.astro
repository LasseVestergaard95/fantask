---
---

<section class="banner">
    <img class="img_left" src="samlemaerker.svg">
    <div class="flex">
        <div class="tekst">
            <h1>Er du fan af Fantask? Hvorfor ikke blive belønnet for det?</h1>
            <p>For hver <strong>100 kr</strong> du bruger, får du et Fantask samlemærke. <br>
                Saml 40 mærker og få <strong>25%</strong> rabat på dit næste køb. </p>
            <p>Din passion betaler sig her hos Fantask!</p>
        </div>
        <a href="" class="knap">Læs mere</a>
    </div>
    <img class="img_right" src="samlemaerker.svg">
</section>


<style>
    .banner {
        width: 100vw;
        height: fit-content;
        background-color: var(--dark-blue);
        margin-block: 6rem;
        overflow: hidden;
        position: relative;
        padding-block: 3rem;
        padding-inline: 2rem;
    }

    h1 {
        color: var(--white);
    }

    p {
        color: var(--white);
    }

    p:first-of-type {
        margin-block: 2rem;
    }

    .flex {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items:flex-start;
        height: 100%;
    }

    .tekst {
        text-align: left;
    }

    .knap {
        display: block;
        background: none;
        color: var(--white);
        border: 1px solid var(--white);
        text-align: center;
        border-radius: 5px;
        text-decoration: none;
        font-size: 16px;
        font-weight: 400;
        padding: 0.15rem 0.5rem;
        width: fit-content;
        margin-top: 2rem;
    }

    .img_left {
        width: 50%;
        height: 80%;
        position: absolute;
        left: -10%;
        bottom: -33%;
        display: none;
    }

    .img_right {
        transform: scaleX(-1);
        width: 50%;
        height: 80%;
        position: absolute;
        right: -10%;
        bottom: -33%;
    }
    
    @media screen and (min-width: 768px) {

        .flex {
            justify-content: center;
            align-items: center;
            height: 100%;
        }

        .tekst {
            text-align: center;
        }

        p {
            font-size: 20px;
        }

        .knap {
            margin: 0 auto;
            margin-top: 2rem;
        }

        .img_left {
            bottom: -16%;
            display: block;
        }

        .img_right {
            bottom: -16%;
        }
    }


</style>